import React from "react";
import { View, Text } from "react-native";
import Page from "./Page";

const Quiz: React.FC = () => {
  return (
    <View>
      <Page />
    </View>
  );
};

export default Quiz;
